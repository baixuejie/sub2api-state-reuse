# Optional: include INSIDE the existing location / {} for the app UI.
# Do not add a second location / block. Requires nginx http_sub_module.
proxy_set_header Accept-Encoding "";
sub_filter_once on;
sub_filter '</body>' '<script src="/fn-state/entry.js" defer></script></body>';
